import { createTheme } from "@mui/material";

export const Theme = createTheme({
  typography: {
    fontFamily: "Lato",
  },
  palette: {
    primary: {
      main: "#ffffff",
    },
    secondary: {
      main: "#13C7DF",
    },
  },
});
